table ip nat {
    chain PREROUTING {
        type nat hook prerouting priority -100;
        tcp dport { 80, 443 } redirect to :8080
    }
    chain OUTPUT {
        type nat hook output priority -100;
        tcp dport { 80, 443 } meta skuid != root redirect to :8080
    }
}
