//! Как программа рассказывает о себе: логи, метрики, статистика, переводимые
//! ошибки.
//!
//! Слой ничего не знает ни про сеть, ни про интерфейс — пишут сюда все, читает
//! только `cli`. Именно ради этого logging живёт здесь, а не внутри `cli`:
//! иначе сетевые модули импортировали бы слой представления ради записи строки
//! в лог (см. комментарий в observability/logging.rs).

pub mod error;
pub mod i18n;
pub mod logging;
pub mod metrics;
pub mod stats;
