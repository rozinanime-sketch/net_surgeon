// quic_parser остался после удаления форвардера udp/quic.rs: распознавание
// QUIC переехало в socks5/udp.rs, где адрес назначения известен из заголовка
// SOCKS5. Сам форвардер был построен вокруг неправильной точки интеграции —
// он не мог узнать, куда клиент хотел подключиться, потому что в QUIC имя
// хоста лежит внутри зашифрованного Initial.
pub mod quic_parser;
/// Отправка по UDP-сессии — общая для SOCKS5 UDP и прозрачного режима.
pub mod session;
